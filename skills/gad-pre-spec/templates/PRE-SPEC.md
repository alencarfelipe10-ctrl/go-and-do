# Fase {{NN}} — {{NOME}} — PRE-SPEC
**Data:** {{DATA}} · **Autor:** Claude a pedido do dono · **Status:** insumo de intenção

> Insumo para `gsd-spec-phase`/`gsd-discuss-phase` via /go-and-do. NÃO é o SPEC: não trava requisitos
> nem tem gate de ambiguidade. Pessoas aparecem só por número/papel.

## 1. A fase em uma página (versão para leigo)
<!-- O que a fase entrega, em linguagem de dono de negócio: o problema, o que muda na prática,
     como se percebe que funcionou. Sem jargão, sem nome de arquivo. ≤ 15 linhas. -->

— nada —

## 2. Origem — o fato que motivou (fonte, data)
<!-- O gatilho externo: reunião, laudo, transcrição, débito conhecido, auditoria.
     Uma linha por fato: `<data> · <fonte> → <o que se soube>`. -->

— nada —

## 3. O que já existe no código (arquivo:linha)
<!-- Estado atual do motor, VERIFICADO por leitura direta. Toda afirmação com `arquivo:linha`.
     Nada de "provavelmente" — o que não foi lido não entra aqui. -->

— nada —

## 4. Medições
<!-- Uma linha por medição: `o que se mediu · como (comando executado / arquivo:linha / API real) · resultado`.
     Número sem fonte executada nesta sessão entra marcado `[herdado]` e NUNCA vira `fato_medido`
     no bloco `gad:decisoes`. -->

| O que se mediu | Como (fonte executada) | Resultado |
|---|---|---|
| — nada — | — | — |

## 5. Decisões do dono ({{DATA}})
<!-- Uma linha por decisão travada. `Reversibilidade`: reversible | costly | one-way —
     costly/one-way exigem justificativa (o bloco reprova sem ela). -->

| # | Decisão | Opções descartadas | Consequência | Reversibilidade |
|---|---|---|---|---|
| — nada — | — | — | — | — |

## 6. Hipóteses falsificadas / opções descartadas
<!-- Memória do porquê. Isto NÃO é pergunta em aberto: a spec e o revisor adversarial não devem reabrir. -->

— nada —

## 7. Regras de negócio ditadas pelo cliente (citação → regra)
<!-- `"<citação literal, sem nome de pessoa>" (papel, data) → <regra derivada>`.
     Quem falou aparece por papel ou número ("o cliente", "o responsável 1"), nunca por nome. -->

— nada —

## 8. Fora de escopo
<!-- O que o dono deixou de fora, com o destino: "gate da Fase X", "milestone futuro", "não faremos". -->

— nada —

## 9. Aberto deliberadamente — o que o spec/discuss deve fechar
<!-- O que o dono mandou NÃO decidir aqui. Vira sino na etapa de intenção. -->

— nada —

## 10. Ressalvas (o que as medições NÃO provam)
<!-- Limite de cada medição. Toda entrada com `ressalva` no bloco obriga uma linha
     em "Limitações declaradas" no SPEC (R7) — se você escreve a ressalva, o SPEC vai ter de declará-la. -->

— nada —

## 11. Referências
<!-- Arquivos, commits, documentos e insumos externos usados. Caminhos, não conteúdo. -->

— nada —

<!-- gad:decisoes:begin v1 -->
[]
<!-- gad:decisoes:end -->
